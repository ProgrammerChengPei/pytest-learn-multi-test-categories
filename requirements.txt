# Pytest Test Framework Dependencies / Pytest 测试框架依赖

# Core testing framework / 核心测试框架
pytest>=8.0.0
pytest-dependency>=0.6.0

# HTML reporting / HTML报告
pytest-html>=4.0.0

# Excel reporting / Excel报告
openpyxl>=3.1.0

# Coverage testing / 覆盖率测试
pytest-cov>=5.0.0

# Async testing (if needed) / 异步测试（如需要）
pytest-asyncio>=0.23.0

# Test discovery improvements / 测试发现改进
pytest-xdist>=3.5.0  # Parallel test execution / 并行测试执行
pytest-timeout>=2.3.0  # Test timeout / 测试超时

# Mocking and fixtures / 模拟和fixtures
pytest-mock>=3.12.0
faker>=25.0.0

# Performance profiling / 性能分析
pytest-profiling>=1.7.0

# Test data management / 测试数据管理
testresources>=2.0.1

# JSON validation / JSON验证
jsonschema>=4.21.0

# Logging enhancements / 日志增强
coloredlogs>=15.0.1
